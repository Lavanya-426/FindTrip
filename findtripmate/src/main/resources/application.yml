spring:
  datasource:
    url: jdbc:h2:mem:findtripmate
    driver-class-name: org.h2.Driver
    username: lavanya
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

  sql:
    init:
      mode: always
